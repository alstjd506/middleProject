<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.product.mapper.ProductMapper">
	<select id="productList" resultType="product">
		select *
		from sm_products
		order by product_sales desc
	</select>
	<select id="productNewList" resultType="product">
		select *
		from sm_products
		order by registration_date desc
	</select>
	<select id ="categoryList" parameterType = "int" resultType="product">
		select p.*
		from sm_products p
		join sm_categories c on p.category_id = c.category_id
		where c.top_category = #{topCategory}
	</select>
	<select id="productMdList" resultType="product">
		select *
		from sm_products
		order by product_price desc
	</select>
	<select id="productCheapList" resultType="product">
		select *
		from sm_products
		<![CDATA[
		where product_price <= 3000
		]]>
	</select>
	
	
</mapper>